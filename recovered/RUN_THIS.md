# Run Full Recovery

Terminal tool execution from chat is being auto-canceled in this session, so run this directly in your terminal:

```bash
cd /workspaces/web
bash recovered/extract_all.sh
```

Then check:

- recovered/EXTRACTION_RESULT.txt
- recovered/all/
- recovered/site/
- recovered/snippets/
